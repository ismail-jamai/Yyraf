# 📚 توثيق مشروع منصة دروبشيبينغ المغرب (Yuraf Network)

## 📋 نظرة عامة

هذا المشروع عبارة عن موقع إلكتروني تفاعلي متعدد الصفحات لمنصة دروبشيبينغ مغربية تسمى **Yuraf Network**. الموقع مصمم بالكامل باللغة العربية مع دعم اللغة الإنجليزية، ويقدم خدمات متكاملة في مجال الدروبشيبينغ، الاستيراد، التخزين، وخدمات تحويل الأموال.

---

## 🛠️ التقنيات المستخدمة

### المكتبات الأساسية
- **React 18.2.0**: مكتبة JavaScript لبناء واجهات المستخدم
- **React Router DOM 6.21.0**: للتوجيه والتنقل بين الصفحات
- **React Icons 5.5.0**: مكتبة للأيقونات
- **Vite 5.0.8**: أداة بناء سريعة وحديثة

### أدوات التطوير
- **Tailwind CSS 3.4.0**: إطار عمل CSS للتصميم
- **PostCSS & Autoprefixer**: معالجة CSS
- **TypeScript Types**: أنواع TypeScript للمكتبات

---

## 📁 بنية المشروع

```
dvv/
├── public/                          # الملفات العامة
├── src/
│   ├── components/                  # المكونات القابلة لإعادة الاستخدام
│   │   ├── Header.jsx              # شريط التنقل الرئيسي
│   │   ├── Footer.jsx              # التذييل
│   │   ├── Hero.jsx                # قسم البطل الرئيسي
│   │   ├── JoinUs.jsx              # قسم الانضمام
│   │   ├── Features.jsx            # قسم المميزات
│   │   ├── Partners.jsx            # قسم الشركاء/المواقع
│   │   ├── HowItWorks.jsx          # قسم كيف يعمل (للصفحة الرئيسية)
│   │   └── ContactCenter.jsx       # صفحة مركز الاتصال
│   │
│   ├── pages/                       # الصفحات الرئيسية
│   │   ├── LandingPage.jsx         # الصفحة الرئيسية
│   │   ├── ServicesPage.jsx        # صفحة الخدمات
│   │   ├── RegisterPage.jsx        # صفحة التسجيل
│   │   ├── FAQPage.jsx             # صفحة الأسئلة الشائعة
│   │   └── HowItWorksPage.jsx      # صفحة كيف تعمل المنصة
│   │
│   ├── context/                     # Context API لإدارة الحالة
│   │   └── LanguageContext.jsx     # إدارة اللغة والتوابع
│   │
│   ├── hooks/                       # Custom Hooks
│   │   └── useScrollAnimation.js   # Hook للحركات عند التمرير
│   │
│   ├── translations/                # ملفات الترجمات
│   │   ├── ar.js                   # الترجمات العربية
│   │   └── en.js                   # الترجمات الإنجليزية
│   │
│   ├── image/                       # الصور
│   │   ├── Importer.jpg
│   │   ├── Drop shipping.jpg
│   │   ├── Stockage.jpg
│   │   └── Transfert d'argent.jpg
│   │
│   ├── App.jsx                      # المكون الرئيسي للتطبيق
│   ├── main.jsx                     # نقطة الدخول الرئيسية
│   └── index.css                    # الأنماط العامة
│
├── index.html                       # ملف HTML الرئيسي
├── package.json                     # ملف التبعيات
├── vite.config.js                   # إعدادات Vite
├── tailwind.config.js               # إعدادات Tailwind CSS
├── postcss.config.js                # إعدادات PostCSS
└── README.md                        # ملف README الأساسي
```

---

## 🎯 المكونات الرئيسية

### 1. Header Component (`src/components/Header.jsx`)

**الوظيفة**: شريط التنقل الرئيسي في جميع الصفحات

**المميزات**:
- قائمة تنقل تفاعلية
- قائمة منسدلة للخدمات (Hover على سطح المكتب)
- مبدل اللغة (العربية/الإنجليزية)
- قائمة متجاوبة للجوال (Hamburger Menu)
- زر CTA للتسجيل

**الحالات**:
- `isMenuOpen`: حالة قائمة الجوال
- `isServicesOpen`: حالة القائمة المنسدلة للخدمات
- `isLanguageOpen`: حالة قائمة اختيار اللغة

**التوجيهات**:
- `/` - الرئيسية
- `/services` - الخدمات
- `/faq` - الأسئلة الشائعة
- `/how-it-works` - كيف تعمل
- `/register` - التسجيل
- `/contact-center` - مركز الاتصال

---

### 2. Footer Component (`src/components/Footer.jsx`)

**الوظيفة**: التذييل الذي يظهر في جميع الصفحات

**المحتوى**:
- روابط سريعة
- معلومات الشركة (العنوان، البريد، الهاتف)
- روابط وسائل التواصل الاجتماعي (Facebook, Instagram, LinkedIn)
- حقوق النشر

---

### 3. Hero Component (`src/components/Hero.jsx`)

**الوظيفة**: القسم الرئيسي في الصفحة الرئيسية

**المحتوى**:
- عنوان رئيسي كبير
- وصف تفصيلي للمنصة
- زر CTA للتسجيل
- فيديو توضيحي من YouTube (iframe)

**Context**: يستخدم `LanguageContext` للترجمة

---

### 4. JoinUs Component (`src/components/JoinUs.jsx`)

**الوظيفة**: قسم للانضمام يقدم نوعين من الخدمات

**المحتوى**:
- بطاقتان: **Drop shipping** و **Supplier**
- كل بطاقة تحتوي على:
  - أيقونة
  - عنوان
  - وصف
  - زر CTA
- قسم تواصل في الأسفل

---

### 5. Features Component (`src/components/Features.jsx`)

**الوظيفة**: عرض مميزات المنصة

**المميزات المعروضة**:
1. بدون مخزون
2. شحن سريع
3. دعم 24/7
4. أسعار جملة تنافسية

**التقنيات**: يستخدم `useScrollAnimation` Hook لحركات التمرير

---

### 6. Partners Component (`src/components/Partners.jsx`)

**الوظيفة**: عرض مواقع/دول الخدمة

**المحتوى**:
- شبكة من الأعلام (11 دولة)
- نص وصفي عن مواقع الخدمة
- زر CTA

**الدول المدرجة**:
- المملكة المغربية (رئيسية)
- الإمارات العربية المتحدة
- قطر
- المملكة العربية السعودية
- سلطنة عمان
- البحرين
- الكويت
- الهند
- ساحل العاج
- مالي
- السنغال

---

### 7. HowItWorks Component (`src/components/HowItWorks.jsx`)

**الوظيفة**: شرح خطوات استخدام المنصة (للصفحة الرئيسية)

**الخطوات**:
1. إنشاء الحساب
2. ربط المتجر أو التسجيل كبائع
3. اختيار أو إضافة المنتجات
4. تنفيذ عمليات البيع والشحن
5. متابعة الطلبات والتقارير

**التصميم**: تصميم خطي عمودي مع أيقونات

---

### 8. ContactCenter Component (`src/components/ContactCenter.jsx`)

**الوظيفة**: صفحة مركز الاتصال والدعم الفني

**المحتوى**:
- معلومات الاتصال (هاتف، بريد إلكتروني، عنوان)
- ساعات العمل
- 3 بطاقات معلوماتية

---

## 📄 الصفحات الرئيسية

### 1. LandingPage (`src/pages/LandingPage.jsx`)

**المسار**: `/`

**المكونات المستخدمة**:
- Header
- Hero
- JoinUs
- Features
- Partners
- Footer

**الوصف**: الصفحة الرئيسية للموقع تحتوي على جميع الأقسام الرئيسية

---

### 2. ServicesPage (`src/pages/ServicesPage.jsx`)

**المسار**: `/services`

**المحتوى**:
- قسم Hero مع عنوان ووصف
- 4 خدمات رئيسية:
  1. **الاستيراد** (`#import`)
  2. **التخزين** (`#storage`)
  3. **الدروبشيبينغ** (`#dropshipping`)
  4. **تحويل الأموال** (`#payment`)

**المميزات**:
- كل خدمة تحتوي على:
  - أيقونة من React Icons
  - عنوان ووصف
  - صورة توضيحية
  - زر CTA للتسجيل
- تصميم متجاوب مع تخطيطات مختلفة للجوال وسطح المكتب

---

### 3. RegisterPage (`src/pages/RegisterPage.jsx`)

**المسار**: `/register`

**الوظيفة**: صفحة تسجيل المستخدمين

**المميزات**:
- نموذج متعدد الخطوات (2 خطوات)
- **الخطوة 1**: جمع المعلومات الأساسية
  - نوع الخدمة (Dropshipping)
  - الاسم الشخصي والعائلي
  - البريد الإلكتروني
  - رقم الهاتف
  - المدينة والعنوان
  - الدولة والرمز البريدي
  - رقم البطاقة الوطنية (CIN)
  - رقم الحساب البنكي (RIB)
  - كلمة السر
- **الخطوة 2**: مراجعة وتأكيد المعلومات
- مؤشر تقدم مرئي
- التحقق من البيانات (validation)

---

### 4. FAQPage (`src/pages/FAQPage.jsx`)

**المسار**: `/faq`

**الوظيفة**: صفحة الأسئلة الشائعة

**المحتوى**:
- 10 أسئلة شائعة مع إجاباتها حول:
  - ما هي Yuraf Network؟
  - كيفية الاستفادة من المنصة
  - خدمات الموردين
  - الاستيراد والتخزين
  - العمليات اللوجستية
  - متابعة الطلبات والمدفوعات
  - تحويل الأرباح
  - تأكيد الطلبات
  - خطط الاشتراك
  - التواصل مع الدعم

---

### 5. HowItWorksPage (`src/pages/HowItWorksPage.jsx`)

**المسار**: `/how-it-works`

**الوظيفة**: شرح تفصيلي لكيفية عمل المنصة

**المحتوى**:
- نفس الخطوات الخمسة من مكون `HowItWorks`
- تصميم مخصص للصفحة مع خط عمودي
- عرض مختلف للجوال وسطح المكتب

---

## 🔄 إدارة الحالة (State Management)

### LanguageContext (`src/context/LanguageContext.jsx`)

**الوظيفة**: إدارة اللغة والتوابع في جميع أنحاء التطبيق

**المميزات**:
- دعم لغتين: العربية (RTL) والإنجليزية (LTR)
- حفظ اختيار اللغة في `localStorage`
- تلقائياً يغير اتجاه الصفحة (`dir`) و `lang` attribute
- دالة `t()` للترجمة
- دالة `toggleLanguage()` للتبديل بين اللغات

**الاستخدام**:
```jsx
import { useLanguage } from '../context/LanguageContext';

const MyComponent = () => {
  const { language, toggleLanguage, t } = useLanguage();
  
  return (
    <div>
      <h1>{t('header.home')}</h1>
      <button onClick={toggleLanguage}>تغيير اللغة</button>
    </div>
  );
};
```

---

## 🎣 Custom Hooks

### useScrollAnimation (`src/hooks/useScrollAnimation.js`)

**الوظيفة**: Hook مخصص لتنشيط الحركات عند التمرير

**الميزات**:
- يستخدم `IntersectionObserver` API
- قابل للتخصيص (threshold, rootMargin, triggerOnce)
- يعيد `[ref, isVisible]`

**الاستخدام**:
```jsx
import { useScrollAnimation } from '../hooks/useScrollAnimation';

const MyComponent = () => {
  const [ref, isVisible] = useScrollAnimation({ threshold: 0.2 });
  
  return (
    <div ref={ref} className={isVisible ? 'visible' : ''}>
      المحتوى
    </div>
  );
};
```

---

## 🌐 نظام الترجمة

### ملفات الترجمة

#### `src/translations/ar.js`
```javascript
export default {
  header: {
    home: 'الرئيسية',
    services: 'خدمات',
    // ...
  },
  hero: {
    title: 'منصتك للانطلاق في عالم التجارة الإلكترونية',
    // ...
  }
};
```

#### `src/translations/en.js`
```javascript
export default {
  header: {
    home: 'Home',
    services: 'Services',
    // ...
  },
  hero: {
    title: 'Your platform to start in e-commerce',
    // ...
  }
};
```

**الاستخدام**:
- استخدم `t('header.home')` للحصول على الترجمة
- تدعم مفاتيح متعددة المستويات (`header.services`)

---

## 🎨 نظام الألوان والتصميم

### Tailwind CSS Configuration

**الألوان المخصصة**:
```javascript
colors: {
  primary: '#3B82F6',           // الأزرق الرئيسي
  'primary-hover': '#2563EB',   // الأزرق عند Hover
  secondary: '#F3F4F6',         // الرمادي الفاتح
  'text-primary': '#374151',    // النص الرئيسي
  'text-secondary': '#6B7280',  // النص الثانوي
  'footer-bg': '#1F1F1F',       // خلفية Footer
}
```

**الخطوط**:
- Arabic Fonts: `Cairo`, `Tajawal`, `Noto Kufi Arabic`

**الحركات (Animations)**:
- `animate-fade-in`: ظهور تدريجي
- `animate-slide-up`: انزلاق من الأسفل
- `scroll-animate`: حركة عند التمرير

---

## 🛣️ التوجيه (Routing)

### Routes في `App.jsx`

```javascript
<Routes>
  <Route path="/" element={<LandingPage />} />
  <Route path="/register" element={<RegisterPage />} />
  <Route path="/services" element={<ServicesPage />} />
  <Route path="/faq" element={<FAQPage />} />
  <Route path="/how-it-works" element={<HowItWorksPage />} />
  <Route path="/contact-center" element={<ContactCenter />} />
</Routes>
```

---

## 📱 التجاوب (Responsive Design)

### Breakpoints المستخدمة

- **Mobile**: افتراضي (< 640px)
- **Tablet (sm)**: `sm:` (≥ 640px)
- **Desktop (md)**: `md:` (≥ 768px)
- **Large (lg)**: `lg:` (≥ 1024px)
- **XL (xl)**: `xl:` (≥ 1280px)

**أمثلة**:
```jsx
<div className="text-2xl md:text-4xl lg:text-5xl">
  عنوان متجاوب
</div>
```

---

## 🖼️ الصور المستخدمة

جميع الصور موجودة في `src/image/`:
- `Importer.jpg` - صورة خدمة الاستيراد
- `Drop shipping.jpg` - صورة خدمة الدروبشيبينغ
- `Stockage.jpg` - صورة خدمة التخزين
- `Transfert d'argent.jpg` - صورة خدمة تحويل الأموال

---

## ⚙️ الإعدادات والتكوين

### Vite Configuration (`vite.config.js`)
- يستخدم `@vitejs/plugin-react`
- إعدادات افتراضية لـ Vite

### Tailwind Configuration (`tailwind.config.js`)
- محتوى: `index.html` و `src/**/*.{js,ts,jsx,tsx}`
- ألوان وخطوط مخصصة

### PostCSS Configuration (`postcss.config.js`)
- يستخدم Tailwind CSS و Autoprefixer

---

## 🚀 الأوامر المتاحة

```bash
# تثبيت المكتبات
npm install

# تشغيل المشروع في وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# معاينة البناء
npm run preview
```

---

## 🔍 المميزات الرئيسية

### 1. دعم متعدد اللغات
- العربية (RTL) والإنجليزية (LTR)
- تبديل سلس بين اللغات
- حفظ التفضيلات

### 2. تصميم متجاوب
- يعمل على جميع الأجهزة
- تجربة مستخدم ممتازة

### 3. حركات وتأثيرات
- حركات عند التمرير
- انتقالات سلسة
- تحسينات بصرية

### 4. إمكانية الوصول (Accessibility)
- تركيز واضح على العناصر
- دعم لوحة المفاتيح
- هيكل HTML منطقي

### 5. الأداء
- استخدام Vite للبناء السريع
- تحميل كسول (Lazy Loading) للصور
- تحسينات CSS

---

## 📝 ملاحظات مهمة

### 1. ServicesPage
- جميع الخدمات الأربع معروضة بشكل متسلسل
- كل خدمة لها قسمها الخاص مع ID للربط
- روابط في Header تشير إلى `/services` مع hash

### 2. RegisterPage
- نموذج متعدد الخطوات مع التحقق
- حقل `serviceType` موجود لكن فقط Dropshipping متاح حالياً
- التحقق من تطابق كلمة السر يتم في `handleSubmit`

### 3. الترجمة
- حالياً فقط قسم Header و Hero مترجم
- باقي الأقسام مكتوبة مباشرة بالعربية
- يمكن توسيع نظام الترجمة بسهولة

### 4. الصور
- الصور محلية في `src/image/`
- يمكن استبدالها بصور أفضل

---

## 🐛 المشاكل المعروفة والحلول

### 1. ServicesPage لا تعمل
**الحل**: تأكد من:
- أن المسار `/services` موجود في Routes
- أن المكون `ServicesPage` مُصدر بشكل صحيح
- عدم وجود أخطاء في console

### 2. الترجمة غير كاملة
**الحل**: أضف المزيد من المفاتيح في ملفات الترجمة واستخدم `t()`

### 3. الصور لا تظهر
**الحل**: تأكد من:
- مسار الصور صحيح
- الصور موجودة في `src/image/`
- استخدام `import` بدلاً من مسارات نسبية مباشرة

---

## 🔮 التطويرات المستقبلية المقترحة

1. **نظام مصادقة كامل**
   - تسجيل الدخول
   - إدارة المستخدمين
   - لوحة تحكم

2. **تكامل API**
   - ربط مع Backend
   - حفظ بيانات التسجيل
   - جلب البيانات الديناميكية

3. **توسيع نظام الترجمة**
   - ترجمة جميع النصوص
   - إضافة المزيد من اللغات

4. **تحسينات الأداء**
   - Code Splitting
   - Image Optimization
   - Caching Strategies

5. **اختبارات**
   - Unit Tests
   - Integration Tests
   - E2E Tests

---

## 📞 معلومات الاتصال والتواصل

- **Email**: contact@yurafnetwork.com
- **Phone**: +212 600 000000
- **Location**: الدار البيضاء، المغرب

---

## 📄 الرخصة

جميع الحقوق محفوظة © 2025 Yuraf Network / دروبشيبينغ المغرب

---

## 📚 الموارد الإضافية

- [React Documentation](https://react.dev/)
- [React Router Documentation](https://reactrouter.com/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)
- [Vite Documentation](https://vitejs.dev/)

---

**آخر تحديث**: يناير 2025

